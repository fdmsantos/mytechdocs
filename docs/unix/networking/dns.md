# DNS

## Types name resolution

* Iterative Name Resolution
  * Most common, is client responsible continue querying until get the desired information.
* Recursive Name Resolution
  * DNS Resolver responsible to query the DNS servers until get the IP Address.

## Cache

Supporting negative caching is cache the failed dns queries.