Cortex Cloud Identity Security (módulo principal)
├── CIEM
├── ISPM  
├── DAG
└── ITDR (add-on) ← o que tu queres ativar
    └── Identity Analytics ← pré-requisito do ITDR
        └── Cloud Identity Engine (DSS) ← pré-requisito do Identity Analytics